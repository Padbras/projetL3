#include "aff_elements.hpp"

using namespace sf;
using namespace std;
/*
//chargement des textures
	Texture t_fondAccueil;
	if (!t_fondAccueil.loadFromFile("../img/fond.png"))
	{
	}
	
void chargerSprite(string chemin){
	Texture tex;
	if (!tex.loadFromFile(chemin)){
	}	
	Sprite spr;
	spr.setTexture(tex);
	
	return spr;
}
*/
